const APP_VERSION="v10.4.2",BUILD_DATE="2026-07-06";
const P="touhan2026",DATA_URL="./data/questions.json",LOGIN="touhan_login_v8",STORE="touhan_answers_v8",CUSTOM="touhan_custom_exams_v8",TIMER_STORE="touhan_timer_v10",EMBED={"meta": {"appName": "登録販売者 試験対策", "version": "10.4.2", "examDates": [{"label": "第1回試験", "date": "2026-08-26"}, {"label": "第2回試験", "date": "2026-09-06"}], "today": {"examId": "day-001", "setId": "day-001-set-1"}}, "exams": [{"id": "day-001", "title": "第1回", "date": "2026/07/04", "sets": [{"id": "day-001-set-1", "title": "第1セット", "note": "全120問中 1/4", "questions": [{"no": 1, "chapter": "第1章", "source": "改題", "answer": "×", "text": "医薬品は、期待される薬効のみをもたらし、有害作用を生じることはない。", "explanation": "医薬品には副作用等の有害作用が起こり得ます。"}, {"no": 2, "chapter": "第1章", "source": "改題", "answer": "○", "text": "一般用医薬品であっても、市販後に有効性や安全性に関する情報が集積され、必要に応じて見直しが行われる。", "explanation": "市販後も安全性情報が収集されます。"}, {"no": 3, "chapter": "第1章", "source": "改題", "answer": "×", "text": "一般用医薬品は、医療用医薬品よりも保健衛生上のリスクが高い。", "explanation": "一般用医薬品は相対的にリスクが低いものです。"}, {"no": 4, "chapter": "第1章", "source": "改題", "answer": "○", "text": "セルフメディケーションとは、軽度な身体の不調について、自らの判断で適切に一般用医薬品等を利用する考え方である。", "explanation": "軽度な不調の自己管理を指します。"}, {"no": 5, "chapter": "第1章", "source": "改題", "answer": "×", "text": "一般用医薬品は、添付文書を確認せず経験だけで使用しても問題ない。", "explanation": "添付文書確認が重要です。"}, {"no": 6, "chapter": "第2章", "source": "改題", "answer": "○", "text": "胃液中の塩酸には、タンパク質の消化を助ける働きがある。", "explanation": "胃酸はタンパク質消化に関与します。"}, {"no": 7, "chapter": "第2章", "source": "改題", "answer": "○", "text": "小腸は栄養素の吸収に重要な役割を果たす。", "explanation": "小腸は吸収の中心です。"}, {"no": 8, "chapter": "第2章", "source": "改題", "answer": "○", "text": "肝臓は胆汁を産生する。", "explanation": "胆汁は肝臓で産生されます。"}, {"no": 9, "chapter": "第2章", "source": "改題", "answer": "○", "text": "腎臓は老廃物の排泄だけでなく、水分や電解質の調節にも関与する。", "explanation": "腎臓は体液調節に関与します。"}, {"no": 10, "chapter": "第2章", "source": "改題", "answer": "×", "text": "心臓は静脈血のみを全身へ送り出している。", "explanation": "左心室は動脈血を全身へ送ります。"}, {"no": 11, "chapter": "第3章", "source": "改題", "answer": "○", "text": "第一類医薬品は、販売時に薬剤師による情報提供が義務付けられている。", "explanation": "第一類は薬剤師対応です。"}, {"no": 12, "chapter": "第3章", "source": "改題", "answer": "×", "text": "要指導医薬品は、インターネット販売できる。", "explanation": "要指導医薬品は対面販売です。"}, {"no": 13, "chapter": "第3章", "source": "改題", "answer": "○", "text": "登録販売者は、第二類医薬品及び第三類医薬品を販売できる。", "explanation": "第二類・第三類を扱えます。"}, {"no": 14, "chapter": "第3章", "source": "改題", "answer": "○", "text": "指定第二類医薬品は、陳列方法に一定の基準が設けられている。", "explanation": "陳列に配慮が必要です。"}, {"no": 15, "chapter": "第3章", "source": "改題", "answer": "○", "text": "医薬品の広告では、虚偽又は誇大な表現は禁止されている。", "explanation": "虚偽・誇大広告は禁止です。"}, {"no": 16, "chapter": "第3章", "source": "改題", "answer": "○", "text": "医薬品販売業者には、苦情処理体制の整備が求められる。", "explanation": "相談・苦情対応が必要です。"}, {"no": 17, "chapter": "第3章", "source": "改題", "answer": "×", "text": "配置販売業では、店舗を設ける必要がある。", "explanation": "配置販売は家庭等へ配置する形態です。"}, {"no": 18, "chapter": "第3章", "source": "改題", "answer": "○", "text": "登録販売者は、一定の実務・業務経験等の要件を満たせば店舗管理者になれる場合がある。", "explanation": "管理者要件を満たせば可能です。"}, {"no": 19, "chapter": "第3章", "source": "改題", "answer": "×", "text": "第一類医薬品は、すべて登録販売者のみが販売できる。", "explanation": "第一類は薬剤師です。"}, {"no": 20, "chapter": "第3章", "source": "改題", "answer": "○", "text": "医薬品副作用被害救済制度は、一定の条件を満たした場合に給付対象となる。", "explanation": "一定条件で救済対象です。"}, {"no": 21, "chapter": "第3章", "source": "改題", "answer": "×", "text": "一般用医薬品は、承認後は安全性の再評価が行われない。", "explanation": "承認後も評価されます。"}, {"no": 22, "chapter": "第3章", "source": "改題", "answer": "○", "text": "一般用医薬品の販売では、購入者からの相談に適切に対応することが重要である。", "explanation": "相談対応は重要です。"}, {"no": 23, "chapter": "第4章", "source": "改題", "answer": "○", "text": "アセトアミノフェンは、解熱鎮痛成分として用いられる。", "explanation": "代表的な解熱鎮痛成分です。"}, {"no": 24, "chapter": "第4章", "source": "改題", "answer": "○", "text": "ロペラミドは止瀉成分である。", "explanation": "止瀉成分です。"}, {"no": 25, "chapter": "第4章", "source": "改題", "answer": "○", "text": "クロルフェニラミンマレイン酸塩は抗ヒスタミン成分である。", "explanation": "抗ヒスタミン成分です。"}, {"no": 26, "chapter": "第4章", "source": "改題", "answer": "○", "text": "センノシドは便秘薬成分として用いられる。", "explanation": "大腸刺激性瀉下成分です。"}, {"no": 27, "chapter": "第5章", "source": "改題", "answer": "○", "text": "医薬品の副作用が疑われる場合には、必要に応じて医療機関の受診を勧める。", "explanation": "受診勧奨が必要な場合があります。"}, {"no": 28, "chapter": "第5章", "source": "改題", "answer": "×", "text": "一般用医薬品は、症状が改善しなくても長期間自己判断で使用し続けることが望ましい。", "explanation": "漫然使用せず受診勧奨が必要です。"}, {"no": 29, "chapter": "第5章", "source": "改題", "answer": "○", "text": "添付文書は、安全に使用するための重要な情報源である。", "explanation": "安全使用の重要情報源です。"}, {"no": 30, "chapter": "第5章", "source": "改題", "answer": "○", "text": "副作用情報は、医薬品の適正使用に役立てられる。", "explanation": "安全対策に活用されます。"}]}, {"id": "day-001-set-2", "title": "第2セット", "note": "未登録", "questions": []}, {"id": "day-001-set-3", "title": "第3セット", "note": "未登録", "questions": []}, {"id": "day-001-set-4", "title": "第4セット", "note": "未登録", "questions": []}]}]};
let data,examId,setId,index=0,examMode=false,timer=null,view="normal",viewList=null,wrongMock=false;
let activeCategory="one_by_one";
let manageFilter="custom",manageSort="new",manageSearch="";
let pendingTimerStart=null;
const CATEGORIES=[{id:"one_by_one",label:"一問一答"},{id:"exam_style",label:"本番形式"}];
let timerState=loadTimerState();
let store=JSON.parse(localStorage.getItem(STORE)||"{}"),custom=JSON.parse(localStorage.getItem(CUSTOM)||"[]");
function defaultTimerState(){return {mode:"30問",duration:1800,remaining:1800,running:false,endAt:null,wrongMock:false,timeUpNotified:false,visible:false}}
function loadTimerState(){try{return {...defaultTimerState(),...JSON.parse(localStorage.getItem(TIMER_STORE)||"{}")}}catch(e){return defaultTimerState()}}
function saveTimer(){localStorage.setItem(TIMER_STORE,JSON.stringify(timerState))}
function catLabel(c){return (CATEGORIES.find(x=>x.id===c)||CATEGORIES[0]).label}
function examCategory(e){return e.category||e.mode||"one_by_one"}
function normalizeExam(e){e.category=examCategory(e);e.category_label=e.category_label||catLabel(e.category);(e.sets||[]).forEach(s=>{s.category=s.category||e.category;s.category_label=s.category_label||e.category_label});return e}
function categoryOptions(){return CATEGORIES.map(c=>`<option value="${c.id}">${c.label}</option>`).join("")}
function categoryExams(){let list=data.exams.filter(e=>examCategory(e)===activeCategory);return list.length?list:data.exams}

const $=id=>document.getElementById(id),esc=s=>String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));
function answerOf(q){return String(q.answer??"")}
function isCorrectAnswer(q,v){return String(v||"")===answerOf(q)}
function isValidAnswer(q,v){if(!v)return true;if(hasChoices(q))return q.choices.slice(0,5).some((c,i)=>choiceId(c,i)===String(v));return v==="○"||v==="×"}
function hasChoices(q){return Array.isArray(q.choices)&&q.choices.length>0}
function choiceText(c){return typeof c==="string"?c:(c.text||"")}
function choiceId(c,i){return String((typeof c==="string"?i+1:(c.id??i+1)))}
function answerLabel(q){if(!hasChoices(q))return answerOf(q);let hit=q.choices.slice(0,5).find((c,i)=>choiceId(c,i)===answerOf(q));return hit?`${answerOf(q)}. ${choiceText(hit)}`:answerOf(q)}
function save(){localStorage.setItem(STORE,JSON.stringify(store))}function saveC(){localStorage.setItem(CUSTOM,JSON.stringify(custom))}function toast(m="コピーしました"){let t=$("toast");t.textContent=m;t.classList.add("show");setTimeout(()=>t.classList.remove("show"),1300)}function today(){return new Date().toLocaleDateString("ja-JP")}function iso(d=new Date()){return d.toISOString().slice(0,10)}function add(d,n){let x=new Date(d);x.setDate(x.getDate()+n);return x.toISOString().slice(0,10)}function exam(){return data.exams.find(e=>e.id===examId)||data.exams[0]}function set(){return exam().sets.find(s=>s.id===setId)||exam().sets[0]}function qs(){return set().questions||[]}
function storeKey(e=examId,s=setId){let ex=data&&data.exams?data.exams.find(x=>x.id===e):null,cat=ex?examCategory(ex):activeCategory;return cat+"::"+e+"::"+s}
function legacyStoreKey(e=examId,s=setId){return e+"::"+s}
function arr(e=examId,s=setId){
  let ex=data.exams.find(x=>x.id===e)||data.exams[0],se=ex.sets.find(x=>x.id===s)||ex.sets[0],list=(se.questions||[]),n=list.length,k=storeKey(e,s),oldK=legacyStoreKey(e,s);
  if(!store[k]&&store[oldK]&&n>0){store[k]=JSON.parse(JSON.stringify(store[oldK]))}
  if(!store[k])store[k]={answers:Array(n).fill(""),graded:false,wrong:[],score:null,updated:null,review:[],manualFeedback:"",chatgptResult:null};
  if(store[k].answers.length!==n)store[k].answers=Array(n).fill("");
  store[k].answers=store[k].answers.map((v,i)=>isValidAnswer(list[i]||{},v)?v:"");
  if(n===0){store[k].answers=[];store[k].graded=false;store[k].wrong=[];store[k].score=null;store[k].review=[]}
  if(!store[k].review)store[k].review=[];
  return store[k]
}
$("loginBtn").onclick=()=>{if($("pass").value===P){localStorage.setItem(LOGIN,"ok");boot()}else $("loginErr").classList.remove("hidden")};$("pass").onkeydown=e=>{if(e.key==="Enter")$("loginBtn").click()};$("lockBtn").onclick=()=>{localStorage.removeItem(LOGIN);location.reload()};
async function boot(){$("login").classList.add("hidden");$("app").classList.remove("hidden");try{try{data=await fetch(DATA_URL,{cache:"no-store"}).then(r=>r.json())}catch(e){data=EMBED}data=JSON.parse(JSON.stringify(data));data.exams=[...data.exams,...custom].slice(0,100).map(normalizeExam);let latest=[...categoryExams()].sort((a,b)=>(b.date||"").localeCompare(a.date||""))[0]||data.exams[0];examId=latest.id;setId=(latest.sets.find(s=>(s.questions||[]).length)||latest.sets[0]).id;$("sub").textContent=data.meta.appName+" "+APP_VERSION+" / Build "+BUILD_DATE;if($("appVersion"))$("appVersion").textContent=APP_VERSION;if($("appBuild"))$("appBuild").textContent=BUILD_DATE;selectors();bind();render()}catch(e){alert("読み込み失敗")}}
function selectors(){
  if($("categorySelect")){$("categorySelect").innerHTML=categoryOptions();$("categorySelect").value=activeCategory;}
  if($("importCategorySelect"))$("importCategorySelect").innerHTML=categoryOptions();
  let list=categoryExams();
  if(!list.some(e=>e.id===examId)){examId=list[0].id;setId=(list[0].sets[0]||{}).id;}
  $("examSelect").innerHTML=list.map(e=>`<option value="${e.id}">${esc(e.title)} ${e.date||""}</option>`).join("");
  $("examSelect").value=examId;
  setOptions();
  $("examSelect").onchange=()=>{examId=$("examSelect").value;setOptions();setId=$("setSelect").value;index=0;normal();render()};
  $("setSelect").onchange=()=>{setId=$("setSelect").value;index=0;normal();render()};
  if($("categorySelect"))$("categorySelect").onchange=()=>{activeCategory=$("categorySelect").value;let list=categoryExams();examId=list[0].id;setId=(list[0].sets[0]||{}).id;index=0;normal();selectors();render()};
}
function setOptions(){let e=exam();$("setSelect").innerHTML=e.sets.map(s=>`<option value="${s.id}">${esc(s.title)}</option>`).join("");if(!e.sets.some(s=>s.id===setId))setId=e.sets[0].id;$("setSelect").value=setId}
function bind(){document.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{document.querySelectorAll("nav button").forEach(x=>x.classList.remove("active"));b.classList.add("active");document.querySelectorAll(".page").forEach(p=>p.classList.add("hidden"));$(b.dataset.tab).classList.remove("hidden");render()});$("startToday").onclick=()=>{index=0;document.querySelector('[data-tab="solve"]').click()};$("prevBtn").onclick=()=>{if(index>0)index--;render()};$("nextBtn").onclick=()=>{if(index<vqs().length-1)index++;render()};$("normalModeBtn").onclick=()=>{normal();render()};$("weakModeBtn").onclick=()=>{view="weak";viewList=weakList();index=0;render()};$("randomModeBtn").onclick=()=>{view="random";viewList=shuffle(qs().map((q,i)=>({q,i})));index=0;render()};$("searchBox").oninput=()=>{view="search";viewList=searchList($("searchBox").value);index=0;render()};$("copyCurrent").onclick=()=>copy(promptCurrent());$("openChatGPT").onclick=async()=>{await copy(promptCurrent());location.href="https://chatgpt.com/"};$("gradeCurrent").onclick=()=>{grade();render();toast("自己採点しました")};$("copyAll").onclick=()=>copy(promptAll());$("showWrong").onclick=wrong;$("getTodayJson").onclick=getPrompt;$("importJson").onclick=importJson;$("copyTemplate").onclick=tpl;$("exportData").onclick=()=>copy(JSON.stringify({answers:store,customExams:custom},null,2));$("restoreData").onclick=restore;$("importResultJson").onclick=importResultJson;$("copyResultTemplate").onclick=copyResultTemplate;$("resetSet").onclick=()=>{if(confirm("リセットしますか？")){delete store[storeKey(examId,setId)];delete store[legacyStoreKey(examId,setId)];save();render()}};$("resetAll").onclick=()=>{if(confirm("全削除しますか？")){store={};custom=[];save();saveC();location.reload()}};$("printQuestions").onclick=printQ;$("printFeedback").onclick=printF;$("startExamMode").onclick=()=>openTimerStart(1800,false,"30問");$("startMockMode").onclick=()=>openTimerStart(7200,false,"120問");$("wrongMockMode").onclick=()=>openTimerStart(3600,true,"苦手");$("finishExamMode").onclick=finish;document.querySelectorAll(".timerPauseBtn").forEach(b=>b.onclick=pauseTimer);document.querySelectorAll(".timerResumeBtn").forEach(b=>b.onclick=resumeTimer);document.querySelectorAll(".timerResetBtn").forEach(b=>b.onclick=resetTimer);document.querySelectorAll(".timerFinishBtn").forEach(b=>b.onclick=finish);document.querySelectorAll(".timerHideBtn").forEach(b=>b.onclick=hideTimer);if($("showTimerBtn"))$("showTimerBtn").onclick=showTimer;startTimerLoop();renderTimerPanels();$("copyAutoFeedback").onclick=()=>copy(feedback());if($("examManageList"))$("examManageList").onclick=manageClick;if($("manageFilter"))$("manageFilter").onchange=()=>{manageFilter=$("manageFilter").value;manageList()};if($("manageSort"))$("manageSort").onchange=()=>{manageSort=$("manageSort").value;manageList()};if($("manageSearch"))$("manageSearch").oninput=()=>{manageSearch=$("manageSearch").value;manageList()};if($("timerStartConfirm"))$("timerStartConfirm").onclick=confirmTimerStart;if($("timerStartCancel"))$("timerStartCancel").onclick=closeTimerStart}
function normal(){view="normal";viewList=null;$("searchBox").value=""}function vqs(){return view==="normal"||!viewList?qs().map((q,i)=>({q,i})):viewList}function weakList(){let w=[];data.exams.forEach(e=>e.sets.forEach(s=>{let st=arr(e.id,s.id),list=s.questions||[];if(st.graded)st.wrong.forEach(i=>list[i]&&w.push({q:list[i],i}))}));return w.length?w:qs().map((q,i)=>({q,i}))}function searchList(term){term=(term||"").trim();if(!term)return qs().map((q,i)=>({q,i}));return qs().map((q,i)=>({q,i})).filter(x=>(x.q.text+x.q.chapter+x.q.source+x.q.explanation).includes(term))}
function render(){if(!data)return;home();question();stats();graphs();calendar();reviews();registered();manageList();radar();renderTimerPanels();$("autoFeedbackBox").textContent=feedback();dueNotice()}
function home(){let e=exam(),s=set(),a=0,t=0;e.sets.forEach(st=>{let x=arr(e.id,st.id).answers;a+=x.filter(Boolean).length;t+=x.length});$("todayLabel").textContent=`${e.date||""} ${e.title} ${s.title}`;$("todayProgress").textContent=a+"/"+t;$("countdown").innerHTML=data.meta.examDates.map(x=>`<b>${x.label}</b><br>${Math.ceil((new Date(x.date)-new Date())/86400000)}日`).join("<hr>");let first=new Date("2026-07-02"),last=new Date("2026-09-06"),now=new Date();$("examProgress").textContent="学習期間進捗："+Math.max(0,Math.min(100,Math.round((now-first)/(last-first)*100)))+"%";$("streak").textContent=streak()+"日";$("examCount").textContent=data.exams.length+"/100";let st=arr(),u=st.answers.filter(x=>!x).length;$("nextAction").innerHTML=u?`未回答が <b>${u}</b> 問あります。`:st.graded?`自己採点済み。不正解 ${st.wrong.length} 問。`:"30問回答済み。採点へ。"}
function question(){
  let list=vqs();
  $("empty").classList.toggle("hidden",list.length>0);
  $("questionCard").classList.toggle("hidden",!list.length);
  if(!list.length){$("qProgress").textContent="0/0";$("unanswered").textContent="未回答 0";$("questionCard").innerHTML="";$("jumpGrid").innerHTML="";return;}
  if(index>=list.length)index=list.length-1;
  let it=list[index],q=it.q,i=it.i,st=arr(),ans=st.answers[i],show=st.graded&&!examMode;
  $("qProgress").textContent=`${index+1}/${list.length}`;
  $("unanswered").textContent=`未回答 ${st.answers.filter(x=>!x).length}`;
  let meta=[q.chapter,q.source||q.question_type||"",view].filter(Boolean).join(" / ");
  let choicesHtml="";
  if(hasChoices(q)){
    choicesHtml=`<div class="choices examChoices">${q.choices.slice(0,5).map((c,ci)=>{
      let id=choiceId(c,ci),txt=choiceText(c);
      return `<button class="choice examChoice ${ans===id?"selected":""}" data-v="${esc(id)}"><span class="choiceId">${esc(id)}</span><span class="choiceText">${esc(txt)}</span></button>`;
    }).join("")}</div>`;
  }else{
    choicesHtml=`<div class="choices"><button class="choice ok ${ans==="○"?"selected":""}" data-v="○">○</button><button class="choice ng ${ans==="×"?"selected":""}" data-v="×">×</button></div>`;
  }
  $("questionCard").innerHTML=`<div class="qTop"><div><div class="qNo">問${q.no}</div><div class="badge">${esc(meta)}</div></div><div class="badge">${exam().date||""}</div></div><div class="qText">${esc(q.text)}</div>${choicesHtml}${show?`<div class="result"><b>${isCorrectAnswer(q,ans)?"正解":"不正解"}</b><br>あなた：${esc(ans||"未回答")}<br>正解：${esc(answerLabel(q))}<br>${esc(q.explanation||"")}</div>`:""}`;
  document.querySelectorAll(".choice").forEach(b=>b.onclick=()=>{st.answers[i]=b.dataset.v;st.updated=new Date().toISOString();save();render()});
  $("jumpGrid").innerHTML=list.map((x,j)=>{let c=st.answers[x.i]?"answered":"";if(show)c=isCorrectAnswer(x.q,st.answers[x.i])?"correct":"wrong";if(j===index)c+=" current";return `<button class="jump ${c}" data-i="${j}">${j+1}<br>${st.answers[x.i]||"未"}</button>`}).join("");
  document.querySelectorAll(".jump").forEach(b=>b.onclick=()=>{index=+b.dataset.i;render()})
}
function grade(){let st=arr(),c=0,w=[];qs().forEach((q,i)=>{if(isCorrectAnswer(q,st.answers[i]))c++;else w.push(i)});st.graded=true;st.wrong=w;st.score={correct:c,total:qs().length,rate:qs().length?Math.round(c/qs().length*100):0};st.updated=new Date().toISOString();st.review=w.flatMap(i=>[3,7,14].map(d=>({q:i,due:add(new Date(),d),interval:d,done:false})));save()}
function all(){let total=0,correct=0,ch={},weak={},hist=[];data.exams.forEach(e=>e.sets.forEach(s=>{let st=arr(e.id,s.id),list=s.questions||[];if(st.graded&&st.score){total+=st.score.total;correct+=st.score.correct;hist.push({label:e.title+s.title,rate:st.score.rate});list.forEach((q,i)=>{ch[q.chapter]??={c:0,t:0,w:0};ch[q.chapter].t++;if(isCorrectAnswer(q,st.answers[i]))ch[q.chapter].c++;else{ch[q.chapter].w++;weak[q.chapter]=(weak[q.chapter]||0)+1}})}}));return {total,correct,ch,weak,hist,rate:total?Math.round(correct/total*100):0}}
function stats(){let s=all();$("statsBox").innerHTML=`<div class="statRow"><b>累計</b><span></span><span>${s.rate}%</span></div>`+["第1章","第2章","第3章","第4章","第5章"].map(k=>{let v=s.ch[k]||{c:0,t:0};return `<div class="statRow"><b>${k}</b><span>${v.c}/${v.t}</span><span>${v.t?Math.round(v.c/v.t*100):0}%</span></div>`}).join("")}
function bar(label,rate){return `<div class="barRow"><span>${esc(label).slice(0,8)}</span><div class="barTrack"><div class="barFill ${rate<70?"bad":rate<80?"warn":""}" style="width:${rate}%"></div></div><span>${rate}%</span></div>`}function graphs(){let s=all();$("rateGraph").innerHTML=s.hist.length?s.hist.slice(-10).map(h=>bar(h.label,h.rate)).join(""):"採点後に表示";$("chapterGraph").innerHTML=["第1章","第2章","第3章","第4章","第5章"].map(k=>{let v=s.ch[k]||{c:0,t:0};return bar(k,v.t?Math.round(v.c/v.t*100):0)}).join("");let r=Object.entries(s.weak).sort((a,b)=>b[1]-a[1]);$("weakRanking").innerHTML=r.length?r.map((x,i)=>`<div class="rankItem"><b>${i+1}位 ${x[0]}</b><br>誤答数：${x[1]}</div>`).join(""):"苦手データなし"}
function radar(){let c=$("radarCanvas"),ctx=c.getContext("2d"),s=all(),labs=["第1章","第2章","第3章","第4章","第5章"],cx=160,cy=160,r=110;ctx.clearRect(0,0,320,320);ctx.strokeStyle="#ddd";ctx.fillStyle="#111827";ctx.font="13px sans-serif";for(let ring=1;ring<=5;ring++){ctx.beginPath();labs.forEach((l,i)=>{let a=-Math.PI/2+i*2*Math.PI/5,rr=r*ring/5,x=cx+Math.cos(a)*rr,y=cy+Math.sin(a)*rr;i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.closePath();ctx.stroke()}ctx.beginPath();labs.forEach((l,i)=>{let v=s.ch[l]||{c:0,t:0},rate=v.t?v.c/v.t:0,a=-Math.PI/2+i*2*Math.PI/5,rr=r*rate,x=cx+Math.cos(a)*rr,y=cy+Math.sin(a)*rr;i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.closePath();ctx.fillStyle="rgba(17,24,39,.25)";ctx.fill();ctx.strokeStyle="#111827";ctx.stroke();labs.forEach((l,i)=>{let a=-Math.PI/2+i*2*Math.PI/5,x=cx+Math.cos(a)*(r+24),y=cy+Math.sin(a)*(r+24);ctx.fillStyle="#111827";ctx.fillText(l,x-18,y)})}
function feedback(){let st=arr(),s=all();if(!st.graded)return"現在のセットは未採点です。";let imp=st.chatgptResult;let danger=imp?.dangerKnowledge||Object.entries(s.weak).sort((a,b)=>b[1]-a[1])[0]?.[0]||"大きな偏りなし";let extra=imp?`\n\n【ChatGPT取り込み講評】\n${imp.feedback||""}${imp.nextTasks?`\n\n次回タスク：${imp.nextTasks}`:""}`:"";return`登録販売者 学習フィードバック\n日付：${exam().date||today()}\n${exam().title} ${set().title}\n\n今回正答率：${st.score.rate}%（${st.score.correct}/${st.score.total}）\n累計正答率：${s.rate}%（${s.correct}/${s.total}）\n今日一番危ない知識：${danger}\n次にやること：間違えた問題を3日後・7日後・14日後に復習。${extra}`}
function calendar(){let done=new Set();Object.values(store).forEach(st=>st.updated&&done.add(st.updated.slice(0,10)));let html="";for(let i=27;i>=0;i--){let d=new Date();d.setDate(d.getDate()-i);let x=iso(d);html+=`<div class="dayCell ${done.has(x)?"done":""} ${x===iso()?"today":""}">${d.getMonth()+1}/${d.getDate()}</div>`}$("studyCalendar").innerHTML=html}function streak(){let done=new Set();Object.values(store).forEach(st=>st.updated&&done.add(st.updated.slice(0,10)));let n=0,d=new Date();while(done.has(iso(d))){n++;d.setDate(d.getDate()-1)}return n}
function dueItems(){let items=[];data.exams.forEach(e=>e.sets.forEach(s=>{let st=arr(e.id,s.id),list=s.questions||[];(st.review||[]).forEach(r=>list[r.q]&&items.push({e,s,r,q:list[r.q]}))}));return items.sort((a,b)=>a.r.due.localeCompare(b.r.due))}function reviews(){let items=dueItems();$("reviewSchedule").innerHTML=items.length?items.slice(0,30).map(x=>`<div class="reviewDue"><b>${x.r.due}</b> / ${x.r.interval}日後<br>${x.e.title} ${x.s.title} 問${x.q.no}</div>`).join(""):"復習予定なし"}function dueNotice(){let due=dueItems().filter(x=>x.r.due<=iso());$("dueNotice").classList.toggle("hidden",!due.length);$("dueNotice").innerHTML=due.length?`<b>復習通知</b><br>今日までに復習すべき問題が ${due.length} 問あります。分析 → 復習予定を確認してください。`:""}
function wrong(){let items=[];data.exams.forEach(e=>e.sets.forEach(s=>{let st=arr(e.id,s.id),list=s.questions||[];if(st.graded)st.wrong.forEach(i=>list[i]&&items.push({e,s,q:list[i],ans:st.answers[i]}))}));$("wrongList").innerHTML=items.length?items.map(x=>`<div class="wrongItem"><b>${x.e.title} ${x.s.title} 問${x.q.no}</b><p>${esc(x.q.text)}</p><p>あなた：${x.ans||"未"} / 正解：${esc(answerLabel(x.q))}</p><p class="note">${esc(x.q.explanation||"")}</p></div>`).join(""):"<div class='card'>間違えた問題はまだありません。</div>"}
function ans(allset=false){let e=exam();return allset?["【登録販売者 解答】",`日付：${e.date||today()}`,`${e.title} 全セット`,...e.sets.map(s=>`${s.title}：${arr(e.id,s.id).answers.map(x=>x||"未").join("")}`)].join("\n"):`【登録販売者 解答】\n日付：${e.date||today()}\n${e.title} ${set().title}\n回答：${arr().answers.map(x=>x||"未").join("")}`}function promptCurrent(){return ans(false)+"\n\n今回の正答率、累計正答率、章別正答率、今日一番危ない知識、間違えた問題の短い解説を出してください。"}function promptAll(){return ans(true)+"\n\n120問の総括、章別分析、改善点、翌日の重点学習項目を出してください。"}
function normalizeImportedExam(o){
  if(o.exam)o=o.exam;
  if(!Array.isArray(o.sets)&&Array.isArray(o.questions)){
    o.sets=[{id:(o.id||"custom")+"-set-1",title:o.setTitle||"第1セット",note:o.note||"",questions:o.questions}];
    delete o.questions;
  }
  if(!o.id||!o.title||!Array.isArray(o.sets))throw 0;
  if(!o.date)o.date=today();
  if(!o.category)o.category=$("importCategorySelect")?$("importCategorySelect").value:"one_by_one";
  if(!o.category_label)o.category_label=catLabel(o.category);
  o.sets.forEach((s,si)=>{s.id=s.id||`${o.id}-set-${si+1}`;s.title=s.title||`第${si+1}セット`;s.questions=s.questions||[]});
  return normalizeExam(o);
}
function getPrompt(){let n=Math.min(data.exams.length+1,100),cat=activeCategory,extra=cat==="exam_style"?"各問に no/chapter/theme/knowledge_id/source/question_type/text/choices/answer/explanation を入れてください。choicesは [{id:\"1\",text:\"...\"}] 形式、answerは正解choiceのidにしてください。":"各問に no/chapter/source/answer/text/explanation を入れてください。answerは○または×にしてください。";copy(`登録販売者試験の今日の30問を、アプリ取り込み用JSONだけで作成してください。日付：${today()}、id：day-${String(n).padStart(3,"0")}、title：第${n}回、category：${cat}、category_label：${catLabel(cat)}、setsは第1セットのみ、問題は30問、${extra}公式公開過去問を最優先し、必要なら改題してください。`);document.querySelector('[data-tab="import"]').click()}
function importJson(){try{let o=normalizeImportedExam(JSON.parse($("importBox").value.trim()));let i=custom.findIndex(e=>e.id===o.id);if(i>=0)custom[i]=o;else if(data.exams.length+custom.length<100)custom.push(o);saveC();toast("取り込みました");setTimeout(()=>location.reload(),500)}catch(e){alert("JSON形式が正しくありません")}}
function tpl(){let cat=$("importCategorySelect")?$("importCategorySelect").value:"one_by_one",isExam=cat==="exam_style";copy(JSON.stringify({id:"day-002",title:"第2回",date:today(),category:cat,category_label:catLabel(cat),sets:[{id:"day-002-set-1",title:"第1セット",note:"全120問中 1/4",questions:[isExam?{no:1,chapter:"第1章",theme:"医薬品の本質",knowledge_id:"KNW-CH1-0001",source:"改題",question_type:"single_best",text:"ここに問題文",choices:[{id:"1",text:"選択肢1"},{id:"2",text:"選択肢2"},{id:"3",text:"選択肢3"},{id:"4",text:"選択肢4"}],answer:"1",explanation:"ここに解説"}:{no:1,chapter:"第1章",source:"改題",answer:"○",text:"ここに問題文",explanation:"ここに解説"}]}]},null,2))}
function registered(){$("registeredList").innerHTML=data.exams.map(e=>`<div class="registered"><b>${e.date||""} ${esc(e.title)}</b> <span class="categoryBadge">${catLabel(examCategory(e))}</span><br>${e.sets.map(s=>s.title+"("+(s.questions||[]).length+"問)").join(" / ")}</div>`).join("")}
function isCustomExam(id){return custom.some(e=>e.id===id)}
function storedSetInfo(e,s){let k=storeKey(e.id,s.id),lk=legacyStoreKey(e.id,s.id),st=store[k]||store[lk],n=(s.questions||[]).length;return {answered:st&&Array.isArray(st.answers)?st.answers.filter(Boolean).length:0,total:n,graded:!!(st&&st.graded),hasHistory:!!st}}
function removeStoreForSet(e,s){delete store[storeKey(e.id,s.id)];delete store[legacyStoreKey(e.id,s.id)]}
function chooseSafeExam(){let list=categoryExams();let first=list[0]||data.exams[0];if(first){examId=first.id;setId=(first.sets[0]||{}).id||"";index=0}else{examId="";setId="";index=0}}
function isSampleExam(e){return String(e.title||"").includes("サンプル")||String(e.id||"").includes("sample")||String(e.category||"")==="sample"}
function examQuestionCount(e){return (e.sets||[]).reduce((a,s)=>a+((s.questions||[]).length),0)}
function examSearchText(e){return [e.date,e.title,e.id,examCategory(e),...(e.sets||[]).flatMap(s=>[s.title,s.id,s.note])].join(" ").toLowerCase()}
function filteredManageExams(){
  let q=(manageSearch||"").trim().toLowerCase();
  let list=[...data.exams].filter(e=>{
    let customFlag=isCustomExam(e.id),cat=examCategory(e),sample=isSampleExam(e);
    if(manageFilter==="custom"&&!customFlag)return false;
    if(manageFilter==="official"&&customFlag)return false;
    if(manageFilter==="one_by_one"&&cat!=="one_by_one")return false;
    if(manageFilter==="exam_style"&&cat!=="exam_style")return false;
    if(manageFilter==="sample"&&!sample)return false;
    if(q&&!examSearchText(e).includes(q))return false;
    return true;
  });
  list.sort((a,b)=>{
    if(manageSort==="old")return String(a.date||"").localeCompare(String(b.date||""));
    if(manageSort==="title")return String(a.title||"").localeCompare(String(b.title||""),'ja');
    if(manageSort==="questions")return examQuestionCount(b)-examQuestionCount(a);
    return String(b.date||"").localeCompare(String(a.date||""));
  });
  return list;
}
function manageList(){
  let box=$("examManageList");if(!box||!data)return;
  if($("manageFilter"))$("manageFilter").value=manageFilter;
  if($("manageSort"))$("manageSort").value=manageSort;
  if($("manageSearch"))$("manageSearch").value=manageSearch;
  let exams=filteredManageExams();
  box.innerHTML=exams.map(e=>{
    let customFlag=isCustomExam(e.id),sets=e.sets||[],sample=isSampleExam(e);
    let setRows=sets.map(s=>{let info=storedSetInfo(e,s);return `<div class="manageSet"><div><b>${esc(s.title)}</b> <span class="note">${info.total}問 / 回答${info.answered}/${info.total}${info.graded?" / 採点済み":""}</span></div><div class="manageActions"><button class="smallBtn" data-action="clear-set" data-exam="${esc(e.id)}" data-set="${esc(s.id)}">履歴削除</button>${customFlag?`<button class="smallBtn dangerMini" data-action="delete-set" data-exam="${esc(e.id)}" data-set="${esc(s.id)}">セット削除</button>`:""}</div></div>`}).join("");
    return `<div class="manageExam"><div class="manageHead"><div><b>${esc(e.date||"")} ${esc(e.title)}</b> <span class="categoryBadge">${catLabel(examCategory(e))}</span>${sample?` <span class="sampleBadge">サンプル</span>`:""}<br><span class="note">${customFlag?"追加済み問題（削除可）":"標準内蔵問題（削除不可）"} / 全${examQuestionCount(e)}問</span></div>${customFlag?`<button class="dangerBtn manageDeleteExam" data-action="delete-exam" data-exam="${esc(e.id)}">この問題を削除</button>`:""}</div>${setRows||"<div class='note'>セットなし</div>"}</div>`;
  }).join("") || "<div class='note'>条件に一致する問題がありません。</div>";
}
function manageClick(ev){
  let b=ev.target.closest("button[data-action]");if(!b)return;
  let action=b.dataset.action,eid=b.dataset.exam,sid=b.dataset.set;
  let e=data.exams.find(x=>x.id===eid);if(!e)return;
  if(action==="clear-set"){
    let s=(e.sets||[]).find(x=>x.id===sid);if(!s)return;
    if(confirm(`${e.title} ${s.title} の回答履歴だけ削除しますか？`)){removeStoreForSet(e,s);save();render();toast("回答履歴を削除しました")}
    return;
  }
  if(!isCustomExam(eid)){toast("標準内蔵問題は削除できません");return;}
  if(action==="delete-set"){
    let ci=custom.findIndex(x=>x.id===eid);if(ci<0)return;let si=(custom[ci].sets||[]).findIndex(x=>x.id===sid);if(si<0)return;
    if(confirm(`${custom[ci].title} ${custom[ci].sets[si].title} を削除しますか？`)){
      removeStoreForSet(custom[ci],custom[ci].sets[si]);custom[ci].sets.splice(si,1);if(!custom[ci].sets.length)custom.splice(ci,1);save();saveC();data.exams=data.exams.filter(x=>!(x.id===eid&&(!custom[ci]||!custom.some(c=>c.id===eid))));data.exams=[...data.exams.filter(x=>!isCustomExam(x.id)),...custom].map(normalizeExam);chooseSafeExam();selectors();render();toast("削除しました")
    }
    return;
  }
  if(action==="delete-exam"){
    let ci=custom.findIndex(x=>x.id===eid);if(ci<0)return;
    if(confirm(`${custom[ci].title} を削除しますか？\n回答履歴も削除されます。`)){
      (custom[ci].sets||[]).forEach(s=>removeStoreForSet(custom[ci],s));custom.splice(ci,1);save();saveC();data.exams=data.exams.filter(x=>x.id!==eid);chooseSafeExam();selectors();render();toast("問題を削除しました")
    }
  }
}
function restore(){try{let o=JSON.parse($("restoreBox").value.trim());if(o.answers)store=o.answers;if(o.customExams)custom=o.customExams;save();saveC();location.reload()}catch(e){alert("復元JSONが正しくありません")}}function printQ(){let w=window.open("","_blank"),e=exam();w.document.write(`<html><body><h1>${e.date||""} ${e.title} 問題PDF</h1>${e.sets.map(s=>`<h2>${s.title}</h2>${(s.questions||[]).map(q=>`<div><b>問${q.no}</b> ${q.chapter}/${q.source}<br>${esc(q.text)}${hasChoices(q)?`<ol>${q.choices.slice(0,5).map((c,i)=>`<li>${esc(choiceText(c))}</li>`).join("")}</ol>`:""}<hr></div>`).join("")}`).join("")}</body></html>`);w.document.close();w.print()}function printF(){let w=window.open("","_blank");w.document.write("<pre>"+esc(feedback())+"</pre>");w.document.close();w.print()}function fmt(sec){sec=Math.max(0,Math.ceil(sec||0));return String(Math.floor(sec/60)).padStart(2,"0")+":"+String(sec%60).padStart(2,"0")}
function openTimerStart(sec,wm,label){
  pendingTimerStart={sec,wm,label};
  let min=Math.round(sec/60),total=wm?weakList().length:(label==="120問"?120:(vqs().length||qs().length||30));
  if($("timerStartText"))$("timerStartText").innerHTML=`問題数：<b>${total}</b>問<br>制限時間：<b>${min}</b>分<br>この内容でタイマーを開始しますか？`;
  if($("timerStartMinutes"))$("timerStartMinutes").value=min;
  if($("timerCountdown")){ $("timerCountdown").classList.add("hidden"); $("timerCountdown").textContent="3"; }
  if($("timerStartModal"))$("timerStartModal").classList.remove("hidden");
}
function closeTimerStart(){pendingTimerStart=null;if($("timerStartModal"))$("timerStartModal").classList.add("hidden")}
function confirmTimerStart(){
  if(!pendingTimerStart)return;
  let m=Number($("timerStartMinutes")?$("timerStartMinutes").value:0);
  if(!m||m<1){toast("制限時間を入力してください");return;}
  let sec=Math.round(m*60),wm=pendingTimerStart.wm,label=pendingTimerStart.label;
  if($("timerCountdown"))$("timerCountdown").classList.remove("hidden");
  let n=3;
  let run=()=>{
    if(!$("timerCountdown")){closeTimerStart();startTimer(sec,wm,label);return;}
    $("timerCountdown").textContent=n>0?String(n):"開始";
    if(n-->=0)setTimeout(run,650);
    else{closeTimerStart();startTimer(sec,wm,label)}
  };
  run();
}
function currentRemaining(){if(timerState.running&&timerState.endAt){timerState.remaining=Math.max(0,Math.ceil((timerState.endAt-Date.now())/1000));if(timerState.remaining<=0){timerState.running=false;timerState.endAt=null}}return timerState.remaining}
function timerAnswered(){let list=vqs(),st=arr();return {answered:list.filter(x=>st.answers[x.i]).length,total:list.length||qs().length||0}}
function renderTimerPanels(){
  let rem=currentRemaining(),a=data?timerAnswered():{answered:0,total:30},status=timerState.running?"実行中":(examMode?"一時停止中":"停止中");
  document.querySelectorAll(".timerDisplay").forEach(x=>x.textContent=fmt(rem));
  document.querySelectorAll(".timerStatus").forEach(x=>x.textContent=status);
  document.querySelectorAll(".timerModeLabel").forEach(x=>x.textContent=timerState.mode||"30問");
  document.querySelectorAll(".timerAnswered").forEach(x=>x.textContent=`回答 ${a.answered}/${a.total}`);
  document.querySelectorAll(".timerPanel").forEach(p=>{p.classList.toggle("running",timerState.running);p.classList.toggle("paused",examMode&&!timerState.running);p.classList.toggle("finished",rem<=0&&!timerState.running)});
  document.querySelectorAll('[data-timer-panel="solve"]').forEach(p=>p.classList.toggle("hidden",!timerState.visible));
  if($("timerShowCard"))$("timerShowCard").classList.toggle("hidden",timerState.visible);
}
function startTimerLoop(){if(timer)clearInterval(timer);timer=setInterval(()=>{let before=timerState.remaining,rem=currentRemaining();renderTimerPanels();if(before>0&&rem<=0&&!timerState.timeUpNotified){timerState.timeUpNotified=true;saveTimer();toast("時間切れです");setTimeout(()=>{if(confirm("時間切れです。提出して自己採点しますか？"))finish();else{examMode=false;saveTimer();render()}},100)}} ,1000)}
function startTimer(sec,wm,label){wrongMock=wm;if(wm){view="weak";viewList=weakList();index=0}examMode=true;timerState={mode:label||"30問",duration:sec,remaining:sec,running:true,endAt:Date.now()+sec*1000,wrongMock:wm,timeUpNotified:false,visible:true};saveTimer();document.querySelector('[data-tab="solve"]').click();startTimerLoop();render()}
function pauseTimer(){if(!examMode&&!timerState.running)return;currentRemaining();timerState.running=false;timerState.endAt=null;saveTimer();renderTimerPanels();toast("一時停止しました")}
function resumeTimer(){if(timerState.remaining<=0)return;if(!examMode)examMode=true;timerState.running=true;timerState.endAt=Date.now()+timerState.remaining*1000;saveTimer();startTimerLoop();renderTimerPanels();toast("再開しました")}
function resetTimer(){if(!confirm("タイマーをリセットしますか？"))return;if(timer)clearInterval(timer);timerState={...defaultTimerState()};examMode=false;wrongMock=false;saveTimer();render();toast("リセットしました")}
function hideTimer(){timerState.visible=false;saveTimer();renderTimerPanels();toast("タイマーを非表示にしました")}
function showTimer(){timerState.visible=true;saveTimer();renderTimerPanels();toast("タイマーを表示しました")}
function finish(){if(timer)clearInterval(timer);currentRemaining();timerState.running=false;timerState.endAt=null;saveTimer();examMode=false;wrongMock=false;grade();render();toast("自己採点しました")}
function shuffle(a){return [...a].sort(()=>Math.random()-0.5)}function copy(t){return navigator.clipboard.writeText(t).then(()=>toast()).catch(()=>toast("コピー失敗"))}

function importResultJson(){
  try{
    let raw=$("resultImportBox").value.trim();
    if(!raw) return toast("JSONを貼り付けてください");
    let r=JSON.parse(raw);
    if(r.type && r.type!=="touhan_result") throw 0;
    let eId=r.examId||examId, sId=r.setId||setId;
    let ex=data.exams.find(e=>e.id===eId); if(!ex) throw 0;
    let se=ex.sets.find(s=>s.id===sId); if(!se) throw 0;
    let st=arr(eId,sId);
    const importedQuestions=se.questions||[];
    if(typeof r.answerString==="string"){
      st.answers=r.answerString.split("").map((x,i)=>isValidAnswer(importedQuestions[i]||{},x)?x:"");
    }else if(Array.isArray(r.answers)){
      st.answers=r.answers.map((x,i)=>isValidAnswer(importedQuestions[i]||{},String(x||""))?String(x||""):"");
    }
    let total=(se.questions||[]).length||st.answers.length||0;
    let wrong=Array.isArray(r.wrongNumbers)?r.wrongNumbers.map(n=>Number(n)-1).filter(i=>i>=0):[];
    if(!wrong.length && se.questions && se.questions.length){
      wrong=[];
      se.questions.forEach((q,i)=>{ if(!isCorrectAnswer(q,st.answers[i])) wrong.push(i); });
    }
    let correct=typeof r.correct==="number"?r.correct:total-wrong.length;
    st.graded=true;
    st.wrong=wrong;
    st.score={correct,total,rate:typeof r.rate==="number"?r.rate:(total?Math.round(correct/total*100):0)};
    st.chatgptResult={
      importedAt:new Date().toISOString(),
      date:r.date||ex.date||today(),
      cumulativeRate:r.cumulativeRate??null,
      chapterRates:r.chapterRates||null,
      dangerKnowledge:r.dangerKnowledge||"",
      feedback:r.feedback||"",
      nextTasks:r.nextTasks||"",
      raw:r
    };
    st.manualFeedback=[r.feedback||"",r.dangerKnowledge?("今日一番危ない知識："+r.dangerKnowledge):"",r.nextTasks?("次回タスク："+r.nextTasks):""].filter(Boolean).join("\n\n");
    st.updated=new Date().toISOString();
    st.review=wrong.flatMap(i=>[3,7,14].map(d=>({q:i,due:add(new Date(),d),interval:d,done:false})));
    save();
    examId=eId; setId=sId; index=0;
    selectors(); render();
    toast("採点結果を取り込みました");
  }catch(e){
    alert("採点結果JSONの形式が正しくありません。");
  }
}
function copyResultTemplate(){
  let st=arr();
  let tpl={
    type:"touhan_result",
    date: exam().date || today(),
    examId: examId,
    setId: setId,
    examTitle: exam().title,
    setTitle: set().title,
    answerString: st.answers.map(x=>x||"未").join(""),
    answers: st.answers.map(x=>x||""),
    correct: 0,
    total: (qs()||[]).length || 0,
    rate: 0,
    cumulativeRate: 0,
    chapterRates:{"第1章":0,"第2章":0,"第3章":0,"第4章":0,"第5章":0},
    wrongNumbers:[],
    dangerKnowledge:"ここに今日一番危ない知識",
    feedback:"ここに採点講評",
    nextTasks:"ここに次回の復習・重点項目"
  };
  copy(JSON.stringify(tpl,null,2));
}

if(localStorage.getItem(LOGIN)==="ok")boot();if("serviceWorker"in navigator)window.addEventListener("load",()=>navigator.serviceWorker.register("./service-worker.js").catch(()=>{}));
